{% extends 'base.html.twig' %}

{% block title %}BloggerProfile{% endblock %}

{% block body %}
    <h1>BloggerProfile</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ blogger_profile.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ blogger_profile.name }}</td>
            </tr>
            <tr>
                <th>Bio</th>
                <td>{{ blogger_profile.bio }}</td>
            </tr>
            <tr>
                <th>Niche</th>
                <td>{{ blogger_profile.niche }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ blogger_profile.location }}</td>
            </tr>
            <tr>
                <th>Reach</th>
                <td>{{ blogger_profile.reach }}</td>
            </tr>
            <tr>
                <th>CollaborationsCount</th>
                <td>{{ blogger_profile.collaborationsCount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_blogger_profile_index') }}">back to list</a>

    <a href="{{ path('app_blogger_profile_edit', {'id': blogger_profile.id}) }}">edit</a>

    {{ include('blogger_profile/_delete_form.html.twig') }}
{% endblock %}
